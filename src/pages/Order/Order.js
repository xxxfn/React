import { connect } from 'react-redux';
import OrderUI from './OrderUI';

const mapStateToProps = ({ Order }) => {
  return {}
}

const mapDispatchToProps = (dispatch) => {
  return {}
}

export default connect(mapStateToProps, mapDispatchToProps)(OrderUI);
